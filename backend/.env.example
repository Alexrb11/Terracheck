# ============================================
# Terracheck Backend - Variables de Entorno
# ============================================
# 
# INSTRUCCIONES:
# 1. Copia este archivo como .env: cp .env.example .env
# 2. Edita el archivo .env con tus valores reales
# 3. NUNCA subas el archivo .env al repositorio (está en .gitignore)
#
# ============================================

# MongoDB Connection URI
# Para desarrollo local: mongodb://localhost:27017/terracheck
# Para MongoDB Atlas: mongodb+srv://<usuario>:<password>@cluster.xxxxx.mongodb.net/terracheck
# Para MongoDB con autenticación local: mongodb://usuario:password@localhost:27017/terracheck
MONGO_URI=mongodb://localhost:27017/terracheck

# Puerto del servidor Express
# Por defecto: 3000
PORT=3000

# URL del frontend para configuración de CORS
# En desarrollo: http://localhost:5173 (puerto por defecto de Vite)
# En producción: https://tu-dominio.com
FRONTEND_URL=http://localhost:5173

# JWT Secret - CLAVE SECRETA PARA TOKENS
# ⚠️ IMPORTANTE: Cambiar en producción por una clave segura
# Genera una clave segura con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# O usar: openssl rand -hex 64
JWT_SECRET=terracheck-super-secret-jwt-key-change-in-production

# Entorno de ejecución
# Valores: development | production
# En desarrollo: muestra errores detallados
# En producción: oculta detalles de errores por seguridad
NODE_ENV=development

# MongoDB Connection URI
# Para desarrollo local: mongodb://localhost:27017/terrarium-keeper
# Para MongoDB Atlas: mongodb+srv://<usuario>:<password>@cluster.xxxxx.mongodb.net/terrarium-keeper

MONGO_URI=mongodb://localhost:27017/terrarium-keeper

# Puerto del servidor (opcional, por defecto 3000)
PORT=3000

# URL del frontend para CORS
FRONTEND_URL=http://localhost:5173

# JWT Secret (IMPORTANTE: Cambiar en producción)
# Genera uno seguro con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=terrarium-keeper-super-secret-jwt-key-change-in-production

# ============================================
# Terracheck Frontend - Variables de Entorno
# ============================================
# 
# INSTRUCCIONES:
# 1. Copia este archivo como .env: cp .env.example .env
# 2. Edita el archivo .env con tus valores reales
#
# NOTA: Todas las variables deben comenzar con VITE_ para ser accesibles en el código
#
# ============================================

# URL base del backend API
# En desarrollo: No es necesario si usas el proxy de Vite (configurado en vite.config.ts)
# En producción: URL completa del backend, ej: https://api.tu-dominio.com
# Por defecto: http://localhost:3000
VITE_API_URL=http://localhost:3000

# Base URL para Vue Router (opcional)
# Por defecto: / (raíz)
# Útil si la app se despliega en un subdirectorio, ej: /app
# VITE_BASE_URL=/
